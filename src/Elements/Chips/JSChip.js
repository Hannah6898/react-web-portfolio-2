import React from "react";
import classes from "./Chips.module.css";

const JSChip = () => {
  return (
    <div>
      <button className={classes.js}>JS</button>
    </div>
  );
};

export default JSChip;
