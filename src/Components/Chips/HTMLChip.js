import React from "react";
import classes from "./Chips.module.css";

const HTMLChip = () => {
  return (
    <div>
      <button className={classes.html}>HTML</button>
    </div>
  );
};

export default HTMLChip;
